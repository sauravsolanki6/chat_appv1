import 'package:flutter/material.dart';

class ColorFile {
  final textColor = Color(0xFF111B21); // WhatsApp-like text color
  final textborderColor = Color(0xFFECECEC); // Light gray for borders
  final buttonColor = Color(0xFF008069); // WhatsApp green for buttons
  // Optional addition: a background color for different sections
  final backgroundColor = Color(0xFFF0F0F0); // Light grayish background
  final iconColor = Color(0xFF128C7E); // WhatsApp teal for icons
  final Color whatsAppGreen = Color(0xFF075E54);
  final Color whatsAppLightGreen = Color(0xFF25D366);
  final Color whatsAppGray = Color(0xFF128C7E);
  final Color whatsAppBackground = Color(0xFFECE5DD);
  final Color whatsAppWhite = Colors.white;
  final Color icon = Colors.grey;
}
